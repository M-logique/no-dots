{
  "name": "black-betty",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "build": "wrangler build && cp dist/index.js worker.js",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}